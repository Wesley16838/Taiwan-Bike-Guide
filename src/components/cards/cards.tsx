import { CardProps } from '../../types/components'
import styles from './Card.module.scss'
const Card= ({title, start, end, way, distance, onClick}: CardProps) => {
    return(
        <div className={styles.container}>

        </div>
    )
}

export default Card;